# Dependencies for the rl_nba cross-sell simulation. Needs Python 3.11+.
# Install everything (package runtime + the example notebook) with:
#     pip install -r requirements.txt

# --- Runtime: the simulation, CLI, playback dashboard, and Gym env ---
# (Mirrors [project.dependencies] in pyproject.toml. pyarrow backs the
#  parquet data source; matplotlib draws the learning-curve figure;
#  gymnasium powers the Gym environment in rl_nba.environment.gym_env.)
numpy>=1.26
pandas>=2.1
pyyaml>=6.0
matplotlib>=3.8
pyarrow>=15.0
gymnasium>=0.29

# --- For the example notebook (examples/run_simulation.ipynb) ---
# Gives you a clean Jupyter on a 3.11+ interpreter; harmless no-op if you
# already run Jupyter. Not needed to run the CLI or view the dashboard.
jupyterlab>=4.0
ipykernel>=6.0

# --- Optional: deep-RL agents for the Gym env (PPO / DQN / MaskablePPO) ---
# Heavy (pulls in PyTorch); only needed for the deep-RL bake-off, not for the
# bandits, dashboard, or the Gym env itself. Uncomment to install, or use
# `pip install -e .[rl]`.
# stable-baselines3>=2.0
# sb3-contrib>=2.0
